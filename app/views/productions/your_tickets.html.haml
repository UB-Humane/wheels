- content_for :title, "#{@production.name} — Your Tickets"
= render "bike_requests/list", active_tab: @tab, bike_requests: @bike_requests, pagy: @pagy,
    production_view: true, tab_counts: @tab_counts, tabs: BikeRequest::MECHANIC_STATUSES
